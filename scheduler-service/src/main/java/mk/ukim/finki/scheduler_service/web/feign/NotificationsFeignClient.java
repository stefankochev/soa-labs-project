package mk.ukim.finki.scheduler_service.web.feign;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;

//@FeignClient(name = "notificationServiceClient", url = "${notificationServiceClient.ribbon.listOfServers}")
//public interface NotificationsFeignClient {
//
//    @GetMapping(value = "/api/v1/notifications/scheduled")
//    void sendScheduledNotifications();
//}
